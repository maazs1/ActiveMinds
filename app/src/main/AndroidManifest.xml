<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.activemindsapp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- <uses-permission android:name="android.permission.INTERNET" /> -->
    <!-- for camera feature -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Anorexia"></activity>
        <activity android:name=".Bulimia" />
        <activity android:name=".Binge" />
        <activity android:name=".EatingDisorder" />
        <activity android:name=".Schizophrenia" />
        <activity android:name=".SchizophreniaofAlberta" />
        <activity android:name=".EatingDisorderSupportNetworkofAlberta" />
        <activity android:name=".CounsellingClinicalServices" />
        <activity android:name=".IslamicFamilyServices" />
        <activity android:name=".YWCAEdmonton" />
        <activity android:name=".SexualAssaultCentreofEdmonton" />
        <activity android:name=".PrideCentreofEdmonton" />
        <activity android:name=".UniversityofAlbertaFaculty" />
        <activity android:name=".THEFAMILYCENTRE" />
        <activity android:name=".DropInYEG" />
        <activity android:name=".CommunityCounsellingCentre" />
        <activity android:name=".MomentumWalkInCounselling" />
        <activity android:name=".CornerstoneCounsellingCentre" />
        <activity android:name=".AccessOpenMindsEdmonton" />
        <activity android:name=".HelpLocations" />
        <activity android:name=".BipolarDepressionMania" />
        <activity android:name=".SeasonalAD" />
        <activity android:name=".MajorDepression" />
        <activity android:name=".ObsessiveCompulsive" />
        <activity android:name=".SocialAnxiety" />
        <activity android:name=".GeneralAnxiety" />
        <activity android:name=".PanicAttack" />
        <activity android:name=".frag" />
        <activity android:name=".IssuesPage" />
        <activity android:name=".StressPage" />
        <activity android:name=".DepressionPage" />
        <activity android:name=".AnxietyPage" />
        <activity android:name=".HubPage" />
        <activity android:name=".newFollowing" />
        <activity android:name=".newFollowPage" />
        <!--
     The API key for Google Maps-based APIs is defined as a string resource.
     (See the file "res/values/google_maps_api.xml").
     Note that the API key is linked to the encryption key used to sign the APK.
     You need a different API key for each encryption key, including the release key that is used to
     sign the APK for publishing.
     You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MoodsMapActivity"
            android:label="@string/title_activity_user_feed_map" />
        <activity
            android:name=".FeedMapActivity"
            android:label="@string/title_activity_mood_map" /> <!-- Configure File Provider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.activemindsapp.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

        <activity android:name=".followerActivity" />
        <activity android:name=".FeedPage" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".MapsActivity"
            android:label="MapsActivity" />
        <activity
            android:name=".ShowEventActivity"
            android:label="ShowEventActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CreateEventActivity"
            android:label="CreateEventActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EditEventActivity"
            android:label="@string/title_activity_user_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UserPage"
            android:label="@string/title_activity_user_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NotificationActivity"
            android:label="NotificationActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginPage" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>